{
  "name": "keiko-corp",
  "version": "1.0.0",
  "description": "The greatest company ever created by Bruno",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "purgecss --css ./css/bootstrap.css --content index.html --output ./css/bootstrap_purge.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "purgecss": "^6.0.0"
  }
}
